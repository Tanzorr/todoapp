import { renderTodoList } from './render.js';
import { initPageAction } from './action-button.js';

renderTodoList();

// Initialize floating action button behaviour (navigates to add page on index)
initPageAction();

